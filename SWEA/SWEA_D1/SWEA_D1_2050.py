# 2050. 알파벳을 숫자로 변환

def transform(char):
    result = ''
    for i in range(len(char)):
        char[i]
    return result.rstrip()

char = list(input())

print(transform(char))

# ord(text) : text의 첫 문자를 아스키코드로 변환해주는 함수
# ord('A') = 65